<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nsit.jo.nsitsportsadmin">

    <application
        android:allowBackup="true"
        android:icon="@drawable/icon_admin"
        android:label="@string/app_name"
        android:roundIcon="@drawable/icon_admin"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity
            android:name=".LoginActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".chooseCriteria"
            android:label="@string/title_activity_choose_criteria"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".sportList" />
        <activity android:name=".ChangeOrRemove" />
        <activity android:name=".addSport"></activity>
    </application>

</manifest>